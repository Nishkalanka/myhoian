# netlify.toml

[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[dev]
  functions_port = 8888
  publish = "dist"
  functions = "netlify/functions"
  open_browser = false
